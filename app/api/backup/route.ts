import {NextResponse} from "next/server";import {cookies} from "next/headers";import {isValidSession} from "@/lib/auth";import db from "@/lib/db";
export async function GET(){if(!isValidSession((await cookies()).get("admin_session")?.value))return NextResponse.json({error:"Unauthorized"},{status:401});const tables=["projects","project_media","contacts","page_views","profile","site_settings"];const backup:any={version:1,createdAt:new Date().toISOString(),tables:{}};for(const t of tables){try{backup.tables[t]=db.prepare(`SELECT * FROM ${t}`).all()}catch{backup.tables[t]=[]}}return new NextResponse(JSON.stringify(backup,null,2),{headers:{"Content-Type":"application/json","Content-Disposition":`attachment; filename=portfolio-backup-${Date.now()}.json`}})}
