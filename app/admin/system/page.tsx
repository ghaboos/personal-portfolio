"use client";import{useState}from"react";import AdvancedSettings from"../settings/AdvancedSettings";export default function System(){const[s,setS]=useState('');async function backup(){setS('Preparing backup…');const r=await fetch('/api/backup');if(!r.ok){setS('Backup failed');return}const blob=await r.blob(),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`portfolio-backup-${Date.now()}.json`;a.click();URL.revokeObjectURL(url);setS('Backup downloaded ✓')}return <main className="settingsPage"><AdvancedSettings/><section className="profileManager"><div className="sectionLabel">Backup / Production</div><h2>Protect the work.</h2><div className="systemCards"><button className="saveButton" onClick={backup}>Export full backup</button><a className="button secondary" href="/">Open website ↗</a></div>{s&&<p className="formStatus">{s}</p>}</section></main>}
