"use client";import {useEffect,useState} from "react";
export default function Inbox(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{fetch('/api/contact',{cache:'no-store'}).then(r=>r.ok?r.json():[]).then(setItems)},[]);return <main className="settingsPage"><div className="profileManager"><div className="sectionLabel">Contact Inbox</div><h2>Messages.</h2><div className="inbox">{items.length?items.map(x=><article className="message" key={x.id}><div><strong>{x.name}</strong><span>{x.email}</span></div><b>{x.subject||'No subject'}</b><p>{x.message}</p><small>{x.created_at}</small></article>):<div className="emptyPanel">No messages yet.</div>}</div></div></main>}
