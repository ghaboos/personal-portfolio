import Link from "next/link";import db from "@/lib/db";
export default function ProjectsPage(){const projects:any[]=db.prepare("SELECT * FROM projects WHERE status='published' ORDER BY featured DESC,created_at DESC").all();return <main className="projectsPage"><nav className="projectNav"><Link href="/" className="back">← D.</Link><span>SELECTED WORK</span><span>2026</span></nav><header className="projectsHeader"><div className="sectionLabel">Portfolio / 02</div><h1>Selected<br/><em>work.</em></h1><p>Projects, experiments and things built from curiosity.</p></header><section className="projectList">{projects.map((p,index)=>{const tags=JSON.parse(p.tags||"[]");return <Link href={`/projects/${p.slug}`} key={p.id} className="projectLink"><article className="projectRow"><span className="projectNumber">{String(index+1).padStart(2,"0")}</span><div className="projectVisual">{p.image?<img src={p.image} alt=""/>:<span>{p.title}</span>}</div><div className="projectInfo"><span className="projectType">{p.type}</span><h2>{p.title}</h2><p>{p.description}</p><div className="tags">{tags.map((tag:string)=><span key={tag}>{tag}</span>)}</div></div><div className="projectYear">{p.year}<b>↗</b></div></article></Link>})}</section></main>}
